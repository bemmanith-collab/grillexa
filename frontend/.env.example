# Leave empty to use relative "/api" requests (default — required for the
# production Fly.io image, where Nginx and the backend share one origin).
# Only set this if you deploy the frontend and backend as separate origins.
VITE_API_URL=
